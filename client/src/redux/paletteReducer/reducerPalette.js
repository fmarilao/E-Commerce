const initialState = {
    palette: {
        type: 'light',
        primaryMain: '#F4E6E3',
        secondaryMain: '#C9002D',
        secondaryDarker: '#932020',
        background: '',
    }
  }
  

export default (state = initialState, action) => {
    return state
}